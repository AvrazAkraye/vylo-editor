# الأمان

[English](SAFETY.md) · **العربية** · [کوردیی ناوەندی](SAFETY.ckb.md) · [کوردیا بادینی](SAFETY.kmr.md)

ما الذي يستطيع Vylo Editor فعله بجهازك، وما لا يستطيع.

يشغّل Vylo Editor وكيل برمجة على مجلد تختاره أنت، على حاسوبك أنت. والتصميم كله
يقوم على قاعدة واحدة:

> **لا يصل أي ناتج من النموذج إلى القرص أو إلى الطرفية دون موافقة إنسان — ذلك
> المحتوى وتلك السلسلة بعينها افتراضياً، أو، حين تُفعَّل الموافقة التلقائية، فئة
> من الإجراءات قُرِّرت مسبقاً لتلك الجلسة.**

كانت هذه الجملة أقصر: كانت تقول *قرأ ووافق على ذلك المحتوى بعينه*، بلا استثناء.
أضاف الإصدار 0.52.1 وضعاً يستطيع فيه التطبيق أن يجيب نيابة عنك، فغُيِّرت الجملة
بدل إخفاء الوضع خلفها. ما لم يتغيّر هو من يقرّر: الموافقة التلقائية معطّلة في كل
مرة يبدأ فيها التطبيق، ولا يستطيع تفعيلها إلا إنسان.

يشرح هذا المستند ما تعنيه تلك القاعدة عملياً، وأين تُفرض، ثم — وهو الجزء الذي
يمنح بقيته قيمتها — ما الذي *لا* تغطيه. كل جملة هنا تذكر الملف الذي يجعلها
صحيحة، لتتحقق منها بدل أن تثق بها. وهو يصف الإصدار 0.114.0.

---

## الموافقة المسبقة

**معطّلة في كل مرة يبدأ فيها التطبيق.** لا تُحفظ، وذلك عن قصد: من فعّلها كان
يعرف لماذا، وفتح النافذة نفسها بعد أسبوع والبوابة مرفوعة ليس القرار نفسه.
الإعدادات ← الموافقة، ثلاثة خيارات:

| | ما تفعله |
|---|---|
| **اسألني في كل مرة** | الوضع الافتراضي، وما كان التطبيق يفعله دائماً. |
| **طبّق التعديلات واسأل قبل الأوامر** | تُطبَّق تغييرات الملفات وحدها. كل أمر لا يزال يُسأل عنه. |
| **طبّق التعديلات ونفّذ الأوامر** | تعمل الأوامر أيضاً، عدا ما هو مذكور أدناه. |

وطوال تفعيله تبقى علامة كهرمانية في شريط الحالة، والضغط عليها يعود مباشرة إلى
هذا الإعداد. وكل ما يوافق عليه يُكتب في السجل ويُعلَّم بذلك، فما جرى بينما لم
تكن تقرأ شيء تستطيع قراءته بعدها.

**لا يكسب النموذج شيئاً.** يبقى `apply_write` و`run_command` في Rust وكل أوامر
`pty_*` غائبة عن مخطط الأدوات، تماماً كما كانت (`app/test/modes.test.mjs`).
الموافقة التلقائية تجيب عن الحوار، لا تلتف حوله. كل كتابة لا تزال تمر عبر
`Pending` وكل أمر عبر `askToRun`، ولهذا فإن النقطة التالية صحيحة:

**كل كتابة تلقائية لا تزال تُحفَظ كنقطة استعادة.** يُحتفظ بالمحتوى السابق قبل
نزول الجديد، فالتغيير غير المراقَب تغييرٌ يمكن التراجع عنه
(`app/src-tauri/src/checkpoint.rs`). هذا الوضع مبرَّر لأنه *قابل للعكس*، لا لأنه
مراقَب.

### ما لا يُوافَق عليه تلقائياً أبداً

بعض الأوامر لا يمكن التراجع عنها ولا تنفع معها نقطة استعادة. هذه تسأل دائماً،
عند كل مستوى، و**لا يوجد إعداد يوقف ذلك** (`app/src/auto.ts`،
`app/test/auto.test.mjs`):

- كل ما يحذف — `rm -rf`، `rmdir`، `shred`، `find … -delete`، `git clean`
- كل ما يكتب على قرص أو جهاز مباشرة — `dd`، `mkfs`، `> /dev/…`
- كل ما يغادر هذا الجهاز أو يعيد كتابة تاريخ مشترك — `git push`، `git rebase`،
  `git reset --hard`، `npm publish`
- كل ما يعمل كشخص آخر أو ينفّذ شيئاً منزَّلاً — `sudo`، `curl … | sh`
- كل ما يؤثّر على الجهاز كله — `shutdown`، `systemctl`، `killall`
- كل ما يقول شيئاً لشخص آخر — رسالة واتساب اقترحها الوكيل (`whatsapp_send`).
  هي المدخل الوحيد هنا الذي ليس أمراً طرفياً، ووجودها هنا للسبب نفسه: لا
  تحفظها أي نقطة استعادة، والشخص الذي يقرؤها ليس في الغرفة

تعتمد القائمة على مطابقة النص، فستوقف أحياناً أمراً كان غير ضار. هذا هو الاتجاه
الصحيح للخطأ، وثمن الخطأ حوار واحد.

**«السماح دائماً بهذا» لا يصل إلى تلك القائمة.** يُحسَب الرفض قبل أن يُسمح لأي
شيء بالإجابة نيابةً عنك، والزر لا يُعرَض أصلاً لأمرٍ التقطته القائمة (`askToRun`
في `app/src/App.tsx`). السماح بـ `git push` مرة واحدة لا يمنعه من السؤال في
المرة التالية، ولا شيء يمنعه.

**عند المستوى الثالث يعمل نداء أداة MCP أيضاً.** كل قاعدة أعلاه مكتوبة بحق أمر
صدفة، والنص الذي يبنيه `runTool` لنداء أداة — `server: tool({"the":"arguments"})`،
في `app/src/agent.ts` — لا يطابق أياً منها. لذا أثناء دورك أنت، عند ذلك
المستوى، تعمل أداة خادم مفعَّل دون سؤال، تماماً كأي أمر آخر. دور الروتين هو
الاستثناء، والقسم التالي يقول ذلك.

---

### الروتينات

الروتين هو وكيل وموجز ووقت، ويعمل ما دام التطبيق مفتوحاً سواء كنت أمام لوحة
المفاتيح أم لا (`app/src/routines.ts`، `app/src/agents.ts`). هذا هو الموضع
الوحيد الذي يتصرف فيه التطبيق دون أن يكون شخص قد كتب شيئاً للتو، لذلك يأخذ
أشد قراءة للقاعدة:

- التشغيل غير المراقَب يكون في **وضع السؤال** — قراءة فقط — أياً كان الوضع
  المعطى للوكيل، ما لم تكن الموافقة المسبقة مفعّلة (`modeFor` في
  `app/src/agents.ts`، `app/test/agents.test.mjs`). الموافقة المسبقة قرار
  اتُّخذ سلفاً لهذه الجلسة؛ ويعمل الروتين تحت ذلك القرار ولا أوسع منه.
- قائمة الرفض أعلاه تنطبق على الروتين تماماً كما تنطبق عليك. وكذلك النصف الآخر
  من ذلك الوعد: الروتين لا يستهلك أبداً «السماح دائماً بهذا» الذي منحتَه أمام
  حوارٍ كنتَ جالساً أمامه، لأن ذاك كان شخصاً يجيب على سؤال، وهذا ليس كذلك
  (`askToRun`).
- **التشغيل غير المراقَب لا يستخدم أداة MCP دون سؤال أبداً**، عند أي مستوى —
  وهو ما يعني، مع عدم وجود أحد أمام الحوار، أنه لا يستخدمها إطلاقاً. قائمة
  الرفض مكتوبة بحق أوامر الصدفة ولا تصف أياً من هذه النداءات، وآثار أداة من طرف
  ثالث لا تُرى من اسمها.
- **الروتين يخصّ المجلد الذي أُنشئ فيه.** يسمّي وكيله بعنوانٍ في ملف
  `.vylo/AGENTS.md` الخاص بذلك المشروع، والملف المبدئي يعطي كل مشروع العناوين
  نفسها — لذلك تُرشَّح القائمة حسب `folder` قبل أن يراها المجدول أو أيٌّ من
  اللوحتين (`inFolder` في `app/src/routines.ts`). روتين أُنشئ في مشروع لا يعمل
  أبداً في مشروع آخر. أما الروتينات التي أُنشئت قبل 0.55.0 فلا مجلد لها، وكانت
  تخصّ أيّ مشروع مفتوح — وهو بعينه ما يُغلقه هذا. يتبنّاها أول مشروع يُفتح بعد
  هذا الإصدار، ويقول سطرٌ في النص كم عددها، ومن أرادها في مشروع آخر ينقلها
  بتحريرها هناك (`adopt` في `app/src/routines.ts`).
- **«شغّل الآن» مراقَب.** أنت أمام حوار الموافقة بحكم التعريف، لذا يستخدم
  التشغيل الذي تبدأه بيدك وضع الوكيل نفسه؛ تشغيلات المجدول وحدها هي المقيَّدة
  بالقراءة فقط.
- كل تشغيل محادثة مستقلة، فما فعله الروتين نصٌّ يمكنك فتحه لاحقاً، وكل كتابة
  قام بها محفوظة في نقطة استعادة. وسطره الأول يسمّي الروتين والوكيل والوضع الذي
  أُعطي للتشغيل، فيبقى نصٌّ تقرؤه بعد أسبوع قائلاً ما كان مسموحاً لذلك الوكيل
  أن يفعله حينها، حتى لو عُدِّل `.vylo/AGENTS.md` منذ ذلك الحين.
- **ما دام التشغيل جارياً، فالنافذة له.** فتح مجلد آخر أو بدء محادثة جديدة أو
  فتح محادثة قديمة مرفوض حتى ينتهي — وإلا لتدفّق التشغيل إلى تلك المحادثة
  وحُفِظ فيها بدلاً من محادثته هو. زر الإيقاف ينهيه فوراً.
- التشغيلات التي فات موعدها والتطبيق مغلق يُبلَّغ عنها وتُتخطّى، ولا تُشغَّل
  متأخرة (`missedWhileClosed` في `app/src/routines.ts`): أسبوعٌ من تقارير
  الاثنين ليس شيئاً يُستدرَك يوم الجمعة. وهذا التقرير يشمل عمداً روتينات كل
  المشاريع لا المشروع المفتوح وحده — فما تُخطِّي تشغيلُه قد تُخطّي حيثما كان
  يخصّ — ويُسمَّى كل روتين من مشروع آخر باسم مجلد ذلك المشروع، فيبقى اسمٌ من
  نسخة لا تراها اسماً يمكنك تحديده. أما إشعار لوحة المعلومات فلا متسع فيه لتلك
  التسمية، فيُعطى حصة المشروع المفتوح. ويذهب التقرير إلى النص كما يذهب إلى لوحة
  المعلومات، وهي وحدة يمكنك إيقافها.
- **فتح مشروعٍ يسوّي ما هو مستحقٌّ عليه أصلاً.** النبضة ساعة واحدة للتطبيق كله،
  فالموعد الذي مرّ بينما كان مشروع آخر على الشاشة يقع خلف نافذة الإقلاع: لا
  يبلّغ عنه شيء، وكان الروتين سينطلق لحظة فتح مشروعه، متأخراً أياماً. لذلك يطرح
  فتحُ المجلد السؤالَ نفسه على ذلك المجلد وحده ويجيب عنه بالطريقة نفسها —
  يُبلَّغ عنه ويُتخطّى (`owedNow` في `app/src/routines.ts`).

## ما الذي يخرج إلى الشبكة

يتحدث Vylo Editor إلى بوابة Vylo الخاصة بك، و— فقط إن أضفتَهم — إلى مزوّدي
نماذج تُعِدّهم بنفسك. خارج الصندوق هي خدمة واحدة: واجهة النموذج وواجهة الحساب
كلتاهما البوابة. لا مزوّد هوية من طرف ثالث، ولا تحليلات، ولا تقارير أعطال، ولا
يُتَّصل بأي شيء لم تسمّه أنت — باستثناءين تبدؤهما بيدك ويُشرحان أدناه: لوحة البحث
العلمي تبحث عن المراجع في فهرسين عامّين للإنتاج العلمي، هما OpenAlex وCrossref؛
ولوحة الفيديو تستعلم عن موضوع الفيديو في Wikidata وWikipedia، وتبحث عن الصور
والموسيقى في مجموعتين عامّتين من الوسائط المفتوحة الترخيص، هما Openverse
وWikimedia Commons، وتأخذ خطوط أنماطها من Google Fonts، وتدع Remotion يُحصي كل
فيلم أو صورة غلاف تصدّرها.

**المزوّدون المضافون** (الإعدادات ← الحساب ← مزوّدو النماذج) لكل منهم عنوانه
ومفتاحه، وتحكمهم قاعدة واحدة تثبّتها `app/test/providers.test.mjs`: **لا يُرسل
المفتاح إلا إلى العنوان الذي أُدخل بجانبه.** المفتاح والعنوان يعيشان في سجل
واحد وكل طلب يشتق كليهما من ذلك السجل — لا يوجد مسار يقرن مفتاح مزوّد بعنوان
مزوّد آخر، وإزالة المزوّد تزيل مفتاحه معه. لا يُجلب شيء من أي مزوّد عند بدء
التشغيل أو فتح الإعدادات؛ أول طلب إلى مزوّد هو أول رسالة ترسلها إلى أحد نماذجه.

ولأن العناوين خيارك أنت، فإن قائمة السماح أدناه تسمح الآن بـ `https:` عموماً
(إضافة إلى `http` العادي نحو `localhost` من أجل Ollama وLM Studio). هذا توسيع
حقيقي، وينقل موضع الإنفاذ: ما كانت تثبته السياسة وحدها يثبته الآن
`app/src/providers.ts` — الذي يرفض العناوين غير https ويطبّعها ويوجّه كل طلب —
والاختبارات المكتوبة عليه. التطبيق لا يرسل طلباً إلى أي مكان غير البوابة
والمزوّدين في قائمتك، وOpenAlex وCrossref لمستند بحثي بدأتَه أنت، وطلبات
الاستعلام والصور والموسيقى والخطوط والترخيص المذكورة أدناه لفيديو بدأتَه أنت.

تُصدر الواجهة ثمانية عشر نوعاً من الطلبات الخارجة. ثمانية منها تذهب إلى البوابة،
واثنان إلى ذينك الفهرسين العامّين للإنتاج العلمي، وسبعة للوحة الفيديو — واحد
للاستعلام عن موضوعه، واثنان إلى مجموعتَي الصور، وواحد للموسيقى، وواحد إلى مزوّد
النطق الخاص بك، وواحد للخطوط، وواحد إلى Remotion:

| أين | الطلب | متى |
|---|---|---|
| `app/src/agent.ts` | `POST {gateway}/v1/messages` | كل جولة للوكيل |
| `app/src/inline.ts` | `POST {gateway}/v1/messages` | إعادة صياغة ⌘K، والتطبيق من المحادثة |
| `app/src/complete.ts` | `POST {gateway}/v1/complete` | الإكمال داخل المحرّر (النص الشبحي) |
| `app/src/gateway.ts` | `POST {gateway}/v1/messages` | فحص مفتاح لصقته للتو |
| `app/src/generate.ts` | `POST {gateway}/v1/messages` | كتابة مستند في لوحة البحث العلمي: الخطة، والهيكل، وكل قسم، والمستخلص؛ وللفيديو: تسمية موضوعه، وتخطيط مشاهده، وإعادة مشهد واحد منها، وكتابة تعليقه الصوتي، والردّ على ما تكتبه في تبويب المحادثة فيه؛ وللعرض التقديمي: كتابة شرائحه وملاحظات المتحدث، وإعادة كتابة شريحة واحدة منه، والردّ على ما تقوله أو تكتبه في تبويب المحادثة فيه |
| `app/src/account.ts` | `POST {gateway}/app/api/auth/login` | تسجيل الدخول — و`/auth/register` و`/auth/logout` بالشكل نفسه |
| `app/src/account.ts` | `GET {gateway}/app/api/me` | رصيد الخطة: عند الإقلاع، وعند انتهاء جولة، وإلا فكل خمس دقائق |
| `app/src/account.ts` | `POST {gateway}/app/api/keys` | إنشاء مفتاح التطبيق نفسه، مرة واحدة، في نهاية تسجيل الدخول |
| `app/src/scholar.ts` | `GET https://api.openalex.org/works`، أو `GET https://api.crossref.org/works` حين يرفض OpenAlex أو يتعطّل | البحث عن مراجع لمستند بحثي بدأتَه |
| `app/src/scholar.ts` | `GET https://api.openalex.org/works/doi:{doi}`، `GET https://api.crossref.org/works/{doi}` | الاستعلام عن معرّف DOI أضفتَه إلى مستند بحثي |
| `app/src/videomedia.ts` | `GET https://api.openverse.org/v1/images/`، ثم الصورة المختارة من العنوان الذي يعطيه Openverse لها | البحث عن صورة لمشهد في فيديو بدأتَه، أو حين تطلب صوراً أخرى |
| `app/src/videomedia.ts` | `GET https://commons.wikimedia.org/w/api.php`، ثم الصورة المختارة من `upload.wikimedia.org` | الأمر نفسه، حين يرفض Openverse أو يتعطّل أو لا يجد شيئاً |
| `app/src/videoresearch.ts` | `GET https://www.wikidata.org/w/api.php`، `GET https://{ar,ckb,en,ku}.wikipedia.org/api/rest_v1/page/summary/…`، ثم `commons.wikimedia.org` و`api.openverse.org` لصوره الفوتوغرافية وشعاره | الاستعلام عن موضوع فيديو بدأتَه، قبل تخطيط مشاهده — ما لم تُطفئ **ابحث عن الموضوع على الويب أولاً** — أو حين تطلب الاستعلام عنه مجدداً؛ وللعرض التقديمي شعاره فقط (`findLogo`): اسم الجهة، حين تطلب شعارها من محادثة الشرائح |
| `app/src/videomix.ts` | `GET https://api.openverse.org/v1/audio/`، ثم المقطع المختار من `cdn.freesound.org` أو `*.storage.jamendo.com` أو `upload.wikimedia.org` | البحث عن موسيقى لفيديو، حين تطلبها في تبويب الصوت الخاص به |
| `app/src/videomix.ts` | `POST {speech provider}/v1/audio/speech` | صنع التعليق الصوتي لفيديو، حين تضغط **اصنع الصوت** — إلى مزوّد أضفتَه أنت، بمفتاح ذلك المزوّد نفسه |
| `app/src/videotheme.ts` | `GET https://fonts.gstatic.com/…`، تُصدره `@remotion/google-fonts` | خطوط نمط الفيديو، أول مرة يُعرض فيها أو يُصدَّر في الجلسة |
| `app/src/videoexport.ts` | `POST https://www.remotion.pro/api/track/register-usage-point`، تُصدره `@remotion/web-renderer` | مرة واحدة لكل فيلم (MP4 أو WebM) أو صورة غلاف (PNG) تصدّرها، حين ينتهي أو يفشل |

القيمة الافتراضية لـ `{gateway}` هي `https://capi.vylo-tech.com`، ويمكن تغييرها
في **الإعدادات** (`app/src/App.tsx`).

**تبحث لوحة البحث العلمي عن المراجع في مكانين لم تسمّهما أنت.** لإيجاد أدبيات
المستند، تطلب `app/src/ResearchPanel.tsx` من `app/src/scholar.ts` أن يرسل طلب GET
مجرّداً — بلا مفتاح، ولا ملف تعريف ارتباط، ولا ترويسات من عنده، ولا بريد
إلكتروني — إلى `https://api.openalex.org/works`، وإلى
`https://api.crossref.org/works` حين يرفض OpenAlex أو يتعطّل. كلاهما فهرس عام
للإنتاج العلمي المنشور يجيب أيّ أحد. وما يصلهما هو كلمات البحث، وهي استعلامات
قصيرة استخلصها النموذج من طلبك — أي أن **موضوع مستندك يغادر هذا الجهاز إلى
هاتين الخدمتين.** وإن لم تذكر الخطة كلمات بحث، فلا يُبحث عن شيء حتى تضغط
**ابحث مجدداً**، فيُرسَل عنوان المستند — أو اسم نوعه إن لم يكن له عنوان — ولا
يُرسَل طلبك كما كتبته أبداً. ومعرّف DOI الذي تضيفه إلى مراجع المستند يُستعلَم عنه في
`https://api.openalex.org/works/doi:{doi}`
و`https://api.crossref.org/works/{doi}`. ولا يُرسَل إليهما أي شيء آخر، ولا
يُرسَل إليهما شيء أصلاً ما لم تكن قد بدأتَ مستنداً أو أضفتَ معرّف DOI.

أما الكتابة نفسها فطلبات إلى النموذج كغيرها (`app/src/generate.ts`، الصف
أعلاه). الخطة والهيكل وكل قسم والمستخلص تذهب إلى النموذج الذي اخترتَه لذلك
المستند في اللوحة — نموذج مربع الكتابة ما لم تختر غيره — ويُحدَّد مسارها بـ `route`
نفسها التي يستعملها مربع الكتابة: البوابة، أو أحد مزوّديك، وتحت القاعدة نفسها:
مفتاح تلك الجهة ولا مفتاح غيره. ومع تعدّد الكُتّاب تكون عدة طلبات منها جارية في
وقت واحد. وهي تحمل طلبك، وملاحظاتك وبياناتك، ونصّ ملفات البيانات التي أرفقتَها،
والهيكل، والنص المكتوب حتى تلك اللحظة، وسجلات المراجع التي عُثر عليها. وملف PDF
الذي ترفقه بياناتٍ يُرسَل مرة واحدة إلى النموذج نفسه ليُنسخ نصُّه، ويُحفظ ما يعود
بوصفه نصَّ الملف.

**تبحث لوحة الفيديو عن الصور في مكانين لم تسمّهما أنت.** يخطّط الفيديوَ النموذجُ
الذي اخترتَه في اللوحة، عبر `app/src/generate.ts` و`route` تماماً كما يُكتب
مستند بحثي؛ ويحمل الطلب ما طلبتَه وإعدادات الفيديو، وعند إعادة مشهد: ذلك المشهد
وتعليمتك له. وما يعود مخطط مشاهد (storyboard) — مشاهد من أنواع ثابتة بحقول
مجرّدة: كلمات، وأرقام، وألوان، وبضع كلمات إنجليزية قصيرة يُبحث بها عن صورة — ولا
يكون شيفرة أبداً. يحلّله `app/src/video.ts` ويصلحه، وتُرسم المشاهد بمكوّنات
التطبيق نفسه في `app/src/VideoScenes.tsx`، ولا يُشغَّل شيء كتبه النموذج أبداً.
وللمشهد الذي يحتاج صورة، يرسل `app/src/videomedia.ts` تلك الكلمات الإنجليزية
القصيرة في طلب GET مجرّد — بلا مفتاح، ولا ملف تعريف ارتباط، ولا ترويسات من عنده
— إلى Openverse، وإلى Wikimedia Commons حين يرفض Openverse أو يتعطّل أو لا يجد
شيئاً، ولا يُبقي إلا الصور التي تسمح تراخيصها بإعادة الاستعمال. أي أن **موضوع
فيديوك، في كلمات قليلة، يغادر هذا الجهاز إلى هاتين الخدمتين.** ثم تُنزَّل الصورة
المختارة إلى هذا الجهاز من العنوان الذي تعطيه المجموعة لها — فيعرف مستضيفها
عنوان هذا الجهاز — وتُحفظ في الفيديو مع نسبتها إلى صاحبها: من صنعها، وبأي ترخيص،
وأين وُجدت. وتُعرض هذه النِّسب في بطاقة ختامية ما لم تُطفئها. وخطوط كل نمط تأتي
من Google Fonts، فيعرف أيّ الخطوط طُلبت، ولا شيء عن الفيديو.

**قبل تخطيط الفيديو يُستعلَم عن موضوعه — ما لم تطلب غير ذلك.** يسمّي النموذجُ
الذي اخترتَه ما يدور حوله الطلب — فيصير "UoD" مثلاً "University of Duhok" — عبر
`route` نفسها؛ ثم يرسل `app/src/videoresearch.ts` ذلك الاسم، في طلبات GET مجرّدة
بلا مفتاح ولا ملف تعريف ارتباط ولا ترويسات من عنده، إلى Wikidata، وإلى Wikipedia
العربية أو السورانية أو الإنجليزية أو الكرمانجية طلباً لملخّص قصير، وإلى Wikimedia
Commons وOpenverse طلباً لصوره الفوتوغرافية وشعاره. أي أن **اسم موضوع فيديوك
يغادر هذا الجهاز إلى تلك الخدمات.** ويُعرض ما يعود في تبويب **من الويب** الخاص
بالفيديو — كل حقيقة مع الصفحة التي جاءت منها، وزرّ تبديل لاستبعادها — ولا يصل
إلى النموذج إلا الحقائق التي تُبقيها مُفعّلة، بوصفها الأرقام والتواريخ والأسماء
الوحيدة التي يجوز لمخطط المشاهد أن يقدّمها على أنها حقائق. ولا تُبقى الصور
الفوتوغرافية والشعار إلا إن سمحت تراخيصها بإعادة الاستعمال، وتُنسب إلى أصحابها
كأي صورة أخرى؛ والشعار الذي يُعثر عليه يُقترح عليك للعلامة التجارية، ولا يوضع
فيها أبداً دون أن تطلب. وحين لا يوجد في Wikimedia شعار حرّ — وهذا المعتاد مع
الجامعات — يُقرأ الموقع الإلكتروني للجهة نفسها، أي العنوان الذي يذكره Wikidata لها،
عبر https بحثاً عن الشعار في ترويسته (`siteLogo`)، ويُقترح بالطريقة نفسها ويُنسب إلى
الجهة بوصفه شعارها هي: شعاراً يُستعمل باسمها، لا صورةً مرخّصة ترخيصاً مفتوحاً. وتسأل محادثة الشرائح المكانين أنفسهما عن شعار حين تطلبه منها (`findLogo`) — اسم الجهة فقط، وشعارها فقط: لا حقائق ولا صور — وتضع ما تجده على العرض خطوةً واحدة يمكنك التراجع عنها. وحين يُخاطَب نموذج الفيديو بصيغة Anthropic، قد يحمل طلب
التخطيط أيضاً أداة البحث على الويب الخاصة بـ Anthropic نفسها: فيجري البحث حينها
لدى Anthropic، عبر المسار نفسه، والبوابة التي ترفضها يُحفظ رفضها طوال الجلسة ولا
تُسأل ثانيةً. و**ابحث عن الموضوع على الويب أولاً**، تحت مربع الطلب، مُفعَّل
افتراضياً، وإطفاؤه يوقف هذا كله.

**يمكن أن يكون للفيديو موسيقى وصوت، حين تطلبهما.** في تبويب **الصوت** الخاص
بالفيديو، يبحث `app/src/videomix.ts` في Openverse عن موسيقى مفتوحة الترخيص بكلمات
الطابع التي تختارها — طلب GET مجرّد، كطلبات الصور — ويُنزّل المقطع الذي تختاره
من المضيف الذي يسمّيه Openverse، مع نسبته إلى صاحبه في البطاقة الختامية. أما
التعليق الصوتي فيكتبه النموذج الذي اخترتَه، جملةً لكل مشهد، ولا يُنطق إلا حين
تضغط **اصنع الصوت**: تُرسَل كل جملة، نصّاً، إلى مزوّد نطق أضفتَه في الإعدادات —
عنوان `/v1/audio/speech` بصيغة OpenAI — بمفتاح ذلك المزوّد نفسه، ولا إلى أي عنوان
آخر. ولا يوجد اليوم صوت كردي لدى أي مزوّد، واللوحة تقول ذلك. أما الموسيقى التي
تؤلَّف في تبويب **الصوت** — أو تُطلب في تبويب **المحادثة** — فيكتبها ويعزفها
`app/src/videosynth.ts` على هذا الجهاز، ولا ترسل شيئاً إلى أي مكان. وفي تبويب
**المحادثة** يجيب النموذج بقائمة تغييرات من مجموعة ثابتة — تعديل كلمات مشهد، أو
إضافة مشهد أو حذفه أو نقله، أو النمط، أو الشكل، أو الطول، أو الموسيقى، أو الشعار —
يتحقّق منها `app/src/videochatops.ts` ويطبّقها؛ وهي، كلوحة المشاهد، ليست شيفرة
أبداً، وأي رقم أو تاريخ أو اسم يذكره لا يرد في الطلب أو في الحقائق التي أبقيتها أو
في رسائلك أنت يُرفَض. وحين يحتاج أن يعرف شيئاً أولاً، يطلب من التطبيق البحث عنه —
البحث المذكور أعلاه، بالاسم الذي سأل عنه — ثم يجيب من جديد بما وُجد، مرتين على
الأكثر في الرسالة الواحدة. وإذا طُلب منه، يُنطق التعليق الصوتي — بالطلب نفسه الذي
يرسله **اصنع الصوت** — وإذا طُلب منه تنزيل الفيديو، يضع زر **تنزيل MP4** تحت
جوابه لتضغطه أنت: فهو لا يكتب ملفاً بنفسه أبداً.

**كل فيلم أو صورة غلاف تصدّرها يرسل إلى Remotion حدثَ قياسٍ واحداً.** يُصيَّر
الفيلم — MP4، أو WebM حيث تستطيع النافذة ترميزه — داخل الصفحة بمُصيِّر الويب من
Remotion، `@remotion/web-renderer`، الذي يستدعيه `app/src/videoexport.ts`: كل
إطار يُرسم ويُرمَّز على هذا الجهاز، ولا يُرفع أيٌّ منها؛ وصورة الغلاف إطارٌ واحد
منها، يُرسم بالطريقة نفسها ويُحفظ بصيغة PNG. وحين ينتهي التصيير أو يفشل، يبلّغ المُصيِّر Remotion به، إذ تُحصي Remotion
عمليات التصيير بموجب ترخيصها: طلب `POST` واحد إلى `https://www.remotion.pro`،
يحمل مفتاح الترخيص — وهذا المشروع يستعمل ترخيص Remotion المجاني، ومفتاحه
`free-license`، ويرسله المُصيِّر مفتاحاً فارغاً — وأصلَ الصفحة (origin)، وهل نجح
التصيير، وككل طلب، عنوانَ IP الذي يأتي منه. ليس فيه إطار ولا كلمة ولا عنوان
للفيديو ولا صورة. ولا يستطيع التطبيق إيقافه — فالمُصيِّر يرسله مع كل تصيير — ولا
يُرسَل إلا حين تضغط زراً يُصيِّر: **تنزيل MP4**، أو **حفظ بصيغة MP4…**، أو **فيديو
WebM**، أو **صورة غلاف**، أو **الأشكال الثلاثة كلها** الذي يُصيِّر ثلاثة
أفلام فيرسل ثلاثة أحداث. أما الترجمة النصية ونسخة مخطط المشاهد الاحتياطية فنصٌّ
يُكتب داخل الصفحة، ولا يرسلان شيئاً.

**والثامن عشر هو واتساب، ويذهب إلى حيث ترسله.** كل طلب يُبنى في مكان واحد،
`app/src/whatsappwire.ts`، من نسخة Evolution API تُدخل عنوانها ومفتاحها معاً
في `app/src/WhatsAppPanel.tsx`. ولا يذهب أي طلب على رسائلك إلى غير ذلك
(التفريغ النصي هو الاستثناء الوحيد، وله فقرته أدناه). ملف واحد هو كامل سطح
الشبكة لهذه الميزة، وهو ما يجعل القاعدة أدناه قابلة للتحقق بالقراءة لا بالثقة. تستدعي `/instance/connectionState` للتحقق
مما أدخلته، و`/chat/findMessages` على مؤقّت ما دامت اللوحة مفتوحة،
و`/chat/getBase64FromMediaMessage` عند فتح صورة أو رسالة صوتية أو ملف أو
تسليمها، و`/message/sendText` عند الإرسال، و`/message/sendMedia` أو
`/message/sendWhatsAppAudio` عند إرفاق ملف برسالة، كلٌّ باسم النسخة. والملف
الذي ترفقه يُقرأ من القرص عبر `read_any_file`، ولا يُستدعى إلا من نافذة
الملفات التي فتحتها أنت: النافذة هي الموافقة، ولا شيء يختار مساراً نيابةً عنك. القاعدة هي نفسها التي
ينصّ عليها `app/src/providers.ts` — لا يُرسل المفتاح إلا إلى العنوان الذي
أُدخل بجانبه، وكل طلب يُبنى من ذلك العنوان.

تُجلب الرسائل إلى هذا الجهاز وتُعرض عليه. **يستطيع الوكيل قراءتها، وعبر أدوات
فعّلتها أنت بربط نسخة فقط.** `whatsapp_chats` و`whatsapp_read` في
`app/src/whatsapptool.ts` لا تُعرض على النموذج إلا بعد وجود اتصال، ولا تغيّران
شيئاً ولا تتركان أثراً، فتعملان كما تعمل قراءة ملف. وفي وضع المحادثة، الذي لا
أدوات مشروع فيه، أدوات واتساب هي وحدها المعروضة، وترفض حلقة الوكيل أي أداة لم
تُعرض في الدور مهما طلب النموذج. و*إرسال إلى المحادثة* ما
زال يفعل ما كان يفعله، حين تريد تسليم محادثة واحدة بدل أن يطّلع على الكل.

**يمكن تفريغ الرسالة الصوتية نصياً، وإلى حيث ترسلها أنت فقط.** النموذج لا
يسمع الصوت: واجهته لا تقبله، و`dictate.ts` هو محرّك النطق في
المتصفّح يستمع إلى ميكروفون ولا يمكن توجيهه إلى ملف. فالكلمات تأتي من خدمة
تسمّيها أنت، و`app/src/whatsappvoice.ts` يتحدث إلى نوعين منها.

الأول **Vylo Voice** — `voice.vylo-tech.com` افتراضياً، أو عنوانك أنت — تأذن له
بمفتاحه الخاص `vsk_`. وهو الذي يملك محركات للبادينية والسورانية إلى جانب العربية
والإنجليزية، ويُجرَّب أولاً لأن هذه المحادثات كردية. يضع التسجيل في طابور ويعيد
مهمّة، فترفع اللوحة مرة واحدة ثم تستعلم عن `/api/jobs/{id}` حتى تجهز الكلمات أو
تستسلم. والثاني أي مزوّد بصيغة OpenAI موجود في الإعدادات، يجيب على
`/v1/audio/transcriptions` في نداء واحد ولم يُدرَّب على الكردية قط. وفي الحالتين
يخرج المفتاح والعنوان من سجلّ واحد، تحت القاعدة نفسها: **لا يُرسل المفتاح إلا
إلى العنوان الذي أُدخل بجانبه.**

هذا هو الموضع الوحيد الذي تغادر فيه رسالة واتساب جهازك إلى غير نسختك أنت،
فيستحق التحديد الدقيق. لا يحدث تلقائياً أبداً: لا على مؤقّت، ولا عند فتح اللوحة،
ولا عند تمريرك فوق رسالة صوتية، ولا ضمن *إرسال إلى المحادثة* — بل حين تضغط
"تفريغ نصي" على رسالة بعينها، والزر يسمّي الخدمة التي سيُرسل إليها ذلك التسجيل.
وبلا شيء مُعدّ يعرض الزر أن يأخذك إلى الإعدادات ولا يرفع شيئاً. والرسالة الصوتية
غير المفرّغة تصل إلى النموذج كسطر يقول إنها لم تُسمع.

واللغة خيارك أنت وتُرسل مع الصوت، لأن الكشف التلقائي يخطئ في ثماني ثوانٍ من
هاتف. والكردية مطروحة على Vylo Voice لأن له محركات لها؛ أما على مسار OpenAI
فيُسقَط خيار الكردية بدل إرساله، إذ سيرفضه ذلك النموذج أو يجيب بما يشبه الجواب
فحسب.

كما يستطيع Vylo Voice ترجمة ما سمعه إلى الإنجليزية أو العربية أو أيٍّ من
الكرديتين، في المرور نفسه وعلى الخادم نفسه — بلا خدمة ثانية وبلا رفع إضافي.
ويعود الاثنان ويُحفظ الاثنان: النصّ هو ما قيل، والترجمة قراءة ثانية له، فيُعرضان
خطوتين ويُسلَّمان إلى النموذج سطرين. واستبدال أحدهما بالآخر يخفي تخميناً داخل
تخمين.

**قائمة السماح هي ما يجعل ذلك واقعاً لا نيّة.** سياسة أمان المحتوى في
`app/src-tauri/tauri.conf.json` هي:

```
default-src 'self';
style-src   'self' 'unsafe-inline';
img-src     'self' data: blob:;
media-src   'self' blob:;
connect-src 'self' ipc: http://ipc.localhost
            https: http://localhost:* http://127.0.0.1:*;
frame-src   http://localhost:* http://127.0.0.1:*
            http://[::1]:* http://*.localhost:*
            https://localhost:* https://127.0.0.1:*
            https://[::1]:* https://*.localhost:*
```

`blob:` على `img-src` و`media-src` حتى تتمكن النافذة من عرض بيانات تملكها
أصلاً — صورة واتساب، رسالة صوتية — ويجدر التوضيح أن هذا لا يمنح أي وصول جديد:
عنوان `blob:` كائن صنعته الصفحة من بيانات في ذاكرتها هي، ولا يمكن جلب شيء جديد
عبره. غيابهما كان خللاً لا سياسة: كانت كل صورة وكل رسالة صوتية تُحجب بعد تنزيل
ناجح، ولم تستطع اللوحة الإبلاغ عن ذلك إلا كملف لا يُفتح.

تستطيع النافذة الوصول إلى مضيفي https عموماً — وهو ثمن أن تسمّي مزوّديك بنفسك،
إذ لا يمكن تعديل السياسة أثناء التشغيل. أما أي المضيفين يُتَّصل به *فعلياً*
فيقرّره `app/src/providers.ts`: البوابة، ثم كل مزوّد أضفتَه، ونسخة واتساب إن
ربطتَ واحدة، وكلٌّ بمفتاحه وحده. وتضيف لوحة البحث العلمي مضيفَين لا يختارهما
أحد، هما OpenAlex وCrossref: لا تُبنى عناوينهما إلا في `app/src/scholar.ts`، ولا
يُرسَل إلى أيٍّ منهما أي مفتاح. وتضيف لوحة الفيديو Wikidata وWikipedia وOpenverse
وWikimedia Commons، والمضيفين الذين تُحفظ عندهم صورهما وموسيقاهما، وGoogle Fonts،
وRemotion: لا تُبنى عناوين الاستعلام إلا في `app/src/videoresearch.ts`، ولا عناوين
المجموعتين إلا في `app/src/videomedia.ts` و`app/src/videomix.ts`، ولا عناوين الخطوط
والقياس إلا داخل حزم Remotion نفسها، ولا يُرسَل إلى أيٍّ منها أي مفتاح. والاستثناء
الوحيد هو التعليق الصوتي، الذي يذهب إلى مزوّد النطق الذي أضفتَه، بمفتاحه هو. يظهر `chat.vylo-tech.com` كنص في ثلاث رسائل خطأ (اثنتان في
`app/src/errors.ts` وواحدة في `app/src/gateway.ts`)، لكن لا شيء في التطبيق يرسل
إليه طلباً.

**هناك اتصال واحد يأتي من جانب Rust.** التحديث التلقائي يجلب
`https://capi.vylo-tech.com/updates/{target}/{arch}/{current_version}`، وهو
العنوان المعلَن في `tauri.conf.json`. كل حزمة تحديث موقَّعة بمفتاح minisign،
ونصفه العام مُدمج داخل التطبيق (`pubkey` في الملف نفسه)؛ فحمولة مُلاعَب بها تفشل
في التحقق وتُطرح قبل أن تعمل (`app/src/updates.ts`).

أما النصف الخاص فموجود في مكانين، كلاهما مذكور في `docs/RELEASING.md`: على جهاز
المشرف، وكسرٍّ في GitHub Actions اسمه `TAURI_SIGNING_PRIVATE_KEY` تقرأه مهمة بناء
ويندوز (`.github/workflows/build.yml`). وأسرار GitHub للكتابة فقط، فالـ CI يوقّع
بها ولا يستطيع أحد قراءتها ثانية. وما يهمّك هو أين **لا** يوجد: فهو غائب عن خادم
التحديث عن قصد، حتى لا يكسب مهاجمٌ استولى على قناة التوزيع القدرةَ على توقيع ما
يمرّ فيها.

**ولا شيء غير ذلك.** لا يفتح أي ملف `.rs` في هذا التطبيق مقبساً ولا يُصدر
طلباً، ولا يعلن `app/src-tauri/Cargo.toml` أي عميل HTTP خاص به — والحزمة الوحيدة
فيه التي تتكلم HTTP هي `tauri-plugin-updater`، وعنوانها الوحيد هو الرابط أعلاه.
ويمنح
`app/src-tauri/capabilities/default.json` النافذةَ خمس عشرة صلاحية، ليس من
بينها إضافة HTTP ولا إضافة الطرفية. ولا توجد أي شيفرة قياس أو تحليلات أو تقارير
أعطال في `app/src` ولا في `app/src-tauri/src`؛ وطلب القياس الوحيد الذي يصدره
التطبيق هو طلب مُصيِّر Remotion نفسه، الموصوف أعلاه، مرة لكل فيلم أو صورة غلاف
تُصدَّر.

### ما الذي يوجد فعلاً في الطلب

تحمل جولة الوكيل: موجّه النظام، وكتلة البيئة، وذاكرة المشروع في `VYLO.md` إن
وُجدت، والمحادثة حتى تلك اللحظة، ومحتوى الملفات التي قرأها الوكيل عبر
`read_file`، ونتائج البحث، ومخرجات الأوامر التي وافقتَ عليها، ومخرجات الطرفية
التي ضغطتَ لها **إرسال إلى المحادثة**، والمرفقات التي أضفتَها بنفسك.

كتلة البيئة (`app/src/environment.ts`) مقتضبة عن قصد: نظام التشغيل، والصدفة التي
سيشغّلها `run_command`، و**اسم** المجلد المفتوح — لا مساره المطلق — وأسماء ملفات
الوصف الموجودة، ومدير الحزم المستنتَج من ملف القفل بجوارها، وأسماء المهام أو
السكربتات بعد تمريرها على قائمة سماح. أما الحقول النثرية مثل `description` أو
`author` فلا تُقرأ إطلاقاً.

يرسل ⌘K التحديد، وتعليمتك، وستين سطراً من كل جانب (`CONTEXT_LINES` في
`app/src/inline.ts`)، إضافة إلى ذاكرة المشروع.

**الإكمال داخل المحرّر يرسل الملف كاملاً**، مقسوماً عند المؤشر: `prefix` و
`suffix` و`path` و`language` (`app/src/complete.ts`). الاقتطاع الذي قد تلاحظه في
ذلك الملف يخص *مفتاح الذاكرة المؤقتة*، لا جسم الطلب. وهو مُفعّل افتراضياً، ويعمل
حين تتوقف عن الكتابة، ويُطفأ من الإعدادات (يُحفظ باسم `vylo.autocomplete`).

مشروعك لا يُرفع. ما يخرج هو ما قرأه الوكيل، وقد قرأه لأنه طلبه وأنت تنظر.

**يمكن لمحادثة العروض التقديمية أن تسجّل ما تقوله، وفقط ما دام ميكروفونها
مفتوحاً.** ميكروفونها هو المكان الوحيد الذي يسجّل فيه التطبيق نفسه صوتاً
(`app/src/slidesvoice.ts`): من ضغطتك على الزر إلى ضغطتك الثانية، أو دقيقة، أيّهما
أسبق، ويُظهر النظام أن الميكروفون قيد الاستخدام. يبقى التسجيل في الذاكرة، ويُرسل
إلى خدمة تحويل الكلام إلى نص التي ضبطتَها — Vylo Voice أولاً لأجل السورانية
والبادينية، أو مزوّد بصيغة OpenAI — والتي يسمّيها الزر قبل أن تضغطه، ثم يُتخلّص
منه؛ لا يُكتب شيء على القرص، ولا يُترجم شيء. والكلمات التي تعود تُرسل إلى نموذج
العرض على أنها رسالتك، موسومةً بأنها منطوقة، ولا يغيّر ردّه الشرائح إلا عبر
العمليات التي يفحصها `app/src/slideschat.ts`، ويلغيها تراجع واحد. ويمكنه أن يفتح
شريحة أو يبدأ العرض؛ ولا يمكنه حفظ ملف، بل يعرض الزر الذي يفعل ذلك فقط. وإن لم
تُضبط خدمة كهذه، يستخدم تبويب المحادثة الإملاء المشروح أدناه بدلاً منها.

### الإملاء هو الشيء الوحيد الذي ليس منّا

يستخدم زرّ الميكروفون محرّك التعرّف على الكلام الخاص بالـ webview نفسه —
`webkitSpeechRecognition` في `app/src/dictate.ts`. لا يرسل Vylo أي صوت إلى أي
جهة: المحرّك ملك لنظام التشغيل، وأمّا هل يفرّغ الكلام على الجهاز أم على خوادم
المزوّد فذلك قرار macOS أو Windows، لا قرارنا. وهذا المسار غير مشمول بالسياسة
أعلاه، لأنه ليس طلباً تصدره الصفحة. ولا يُدرَج في مربع الكتابة إلا النتائج
النهائية، ولا شيء ممّا يُسمع يستطيع أن *ينفّذ* شيئاً — فمخرج الإملاء الوحيد هو
مربع نص.

### بيانا اعتمادك، وكلمة مرورك

**مفتاح البوابة** يُحفظ في `localStorage` الخاص بالـ webview تحت الاسم
`vylo.apiKey`، نصاً صريحاً، لا في سلسلة مفاتيح النظام. ويُرسل في ترويسة
`x-api-key` إلى نقاط `/v1/` الخمس أعلاه ولا شيء سواها. يمكنك لصق مفتاح، أو
تسجيل الدخول ليصنع التطبيق مفتاحه بنفسه: يعيد `POST {gateway}/app/api/keys`
المفتاح مرة واحدة فقط، ولا يحفظ الخادم إلا بصمته، فما يعود يُحفظ لحظة وصوله وإلا
ضاع.

**رمز الجلسة** هو JWT، يُحفظ بالطريقة نفسها تحت `vylo.token`
(`app/src/account.ts`)، ويُرسل في ترويسة `Authorization` إلى نقاط `/app/api`
الثلاث أعلاه ولا شيء سواها. هو يوثّق *الحساب*، والمفتاح يوثّق *النموذج*، ولا
يحلّ أحدهما محل الآخر أبداً — وهو ما يؤكده `app/test/account.test.mjs` على السلك
لا في النثر. **تسجيل الخروج يمسح الرمز ولا يمسح المفتاح عمداً**: المفتاح صُنع
لهذا الجهاز ويظل يعمل، وإبطاله قرار يُتخذ في صفحة المفاتيح لا أثر جانبي لإغلاق
جلسة.

**كلمة مرورك لا تُحفظ إطلاقاً.** هي وسيط لدالة واحدة، تُرسل مرة واحدة، ويُسقطها
`app/src/SignIn.tsx` لحظة عودة الطلب — قبل النظر في النتيجة، فلا يبلغها أي مسار
خطأ. ليست في مرجع، ولا في `localStorage`، ولا في أي شيء يُعرض.

ولا شيء من هذا في متناول الوكيل. لا أمر Tauri خلف أي منه، فليس هناك جديد يُستبعد
من مخطط الأدوات أدناه: لا يستطيع النموذج قراءة الرمز، ولا صنع مفتاح، ولا رؤية
كلمة المرور.

تعامل مع الاثنين كما تتعامل مع أي مفتاح API على جهاز تملكه: كل من يستطيع
استخدام حسابك على الجهاز يستطيع قراءتهما.

---

**لوحة خادم التطوير** (`app/src/BrowserPanel.tsx`) تحمّل عنواناً واحداً داخل
`<iframe>` معزول، وعنواناً على هذا الجهاز فقط — `localhost` أو `127.0.0.1` أو
`[::1]` أو اسم `*.localhost`، بأي منفذ — وهو ما يفرضه `app/src/browser.ts` على
كل عنوان يُكتب أو يُعثر عليه في الطرفية أو يُقرأ من التخزين، ويكرره `frame-src`
في السياسة. الصفحة داخل الإطار أصل مختلف: لا تستطيع قراءة تخزين التطبيق حيث
يوجد مفتاحك، ولا جسر أوامر Tauri لها. لا شيء في الإطار يصل إلى النموذج.

## ما الذي يستطيع الكتابة على قرصك

**دالة واحدة تفعل كل ما يكتبه النموذج.** كل بايت من محتوى اقترحه النموذج يمرّ
عبر `apply_write` في `app/src-tauri/src/lib.rs`، ويُبلَغ من الواجهة عبر غلاف واحد
هو `applyWrite` في `app/src/disk.ts`. هذه هي الدعوى التي تقوم عليها القاعدة في
رأس هذه الوثيقة، وهي الجديرة بالفحص.

**وليست هي الشيفرة الوحيدة في هذا التطبيق التي تكتب على القرص**، والقول بغير ذلك
أسهل جملة هنا يمكن دحضها — يكفي `grep` واحد عن `fs::write` في `lib.rs`. والبقية
كلها أشياء تبدؤها *أنت* من زرّ أو عنصر قائمة، وكل واحدة منها غائبة عن مخطط
الأدوات أدناه:

- `create_file` و`create_dir` و`rename_path` و`delete_path` — الإنشاء وإعادة
  التسمية والحذف في شجرة الملفات. لا تحمل أيٌّ منها محتوى؛ الأسماء تأتي من حوار
  كتبتَ فيه، وطرفا إعادة التسمية كلاهما يمرّان عبر `resolve()`.
- `export_write` — نسخة markdown من محادثة، إلى المسار الذي أعادته نافذة الحفظ في
  النظام. هذه إحدى الاثنتين اللتين تضعان *نصاً* كتبه النموذج على القرص خارج
  `apply_write`، وسبب السماح لها بتخطّي الاحتواء مكتوب في تعليق `lib.rs` نفسه
  عليها: المسار مسار نافذة الحفظ، والمحتوى محادثة كنتَ تقرؤها، وما تنتجه سجلّ لا
  شيء يشغّله.
- `export_write_docx` هي الأخرى، ويُسمح لها للأسباب نفسها — مستند Word من لوحة
  البحث العلمي، يُكتب إلى المسار الذي أعادته نافذة الحفظ بعد أن ضغطتَ **حفظ
  بصيغة Word…**. تُبنى بايتاته من المستند نفسه الذي كانت اللوحة تعرضه عليك
  بالضبط. وهي ترفض اسماً لا ينتهي بـ .docx، وبايتات لا تبدأ كما يبدأ أرشيف ZIP،
  ولا تُنشئ أي مجلد.
- `save_pdf` تكتب ملف PDF لمستند البحث العلمي إلى المسار الذي أعادته نافذة الحفظ
  بعد أن تضغط **حفظ بصيغة PDF…** — تطبع واجهة الويب الصفحة إلى ذلك الملف بنفسها،
  بلا نافذة طباعة، بعد أن تضع اللوحة في الصفحة نسخةً ورقية من المستند الذي عُرض
  عليك وتخفي كل ما سواه. وترفض اسماً لا ينتهي بـ .pdf ومجلداً غير موجود، ولا تُنشئ
  مجلدات.
- `export_write_pptx` تكتب ملف PowerPoint من لوحة العروض التقديمية إلى المسار الذي
  أعادته نافذة الحفظ بعد أن تضغط **حفظ بصيغة PowerPoint**، ويُسمح لها لأسباب
  `export_write_docx` نفسها وخلف الحواجز نفسها: تُرفض الأسماء التي لا تنتهي بـ .pptx
  والبايتات التي لا تبدأ كما يبدأ أرشيف ZIP، ولا يُنشأ أي مجلد. تُبنى بايتاتها في
  `app/src/slidespptx.ts` من الشرائح نفسها التي كانت اللوحة تعرضها عليك بالضبط.
  أما **حفظ بصيغة PDF** في اللوحة فهو `save_pdf` مرة أخرى، على صفحات بشكل الشريحة.
- `export_write_video` تكتب ما تصدّره لوحة الفيديو — فيلماً بصيغة MP4 أو WebM، أو
  صورة غلاف بصيغة PNG، أو ترجمة نصية بصيغة SRT، أو مخطط المشاهد بصيغة JSON — بعد
  أن تضغط الزر الخاص بذلك الملف. فـ **حفظ بصيغة MP4…** يكتب إلى المسار الذي أعادته
  نافذة الحفظ، ولا يستبدل ملفاً موجوداً هناك إلا لأن النافذة سألتك أولاً. أما
  **تنزيل MP4** وسائر التنزيلات فتكتب في مجلد التنزيلات، ولا تكتب فوق ملف موجود
  فيه أبداً: إن كان `Title.mp4` مأخوذاً كتبت `Title (2).mp4`، ثم `(3)`، ولا تُنشئ
  الملف إلا إذا لم يكن ثمة شيء بهذا الاسم — ولا تتبع رابطاً رمزياً يحمله. والفيلم
  وصورة الغلاف يُصيَّران داخل الصفحة من مخطط المشاهد نفسه الذي كنتَ تعاينه بالضبط؛
  أما الترجمة النصية وملف مخطط المشاهد فهما كلمات ذلك المخطط نفسها، مكتوبةً نصاً.
  وتصل بايتاته جسماً خاماً للطلب لا نصاً. وترفض مساراً غير مطلق، ومجلداً غير
  موجود، وأي اسم غير .mp4 و.webm و.png و.srt و.json، وبايتات ليست ما يقوله الاسم —
  MP4 بلا علامة `ftyp`، أو WebM بلا ترويسة EBML، أو PNG بلا توقيعه، أو ترجمة نصية
  ليست نص UTF-8 خالصاً، أو مخطط مشاهد ليس JSON — أو بايتات أكثر مما ينبغي: 1
  غيبيبايت للفيلم، و64 ميبيبايت لصورة الغلاف، و5 ميبيبايت للنص. ولا تُنشئ مجلدات.
- `reveal_path` لا تكتب شيئاً ولا تفتح شيئاً: تُظهر ملفاً محدَّداً في Finder أو
  المستكشف — صفاً من شجرة الملفات، أو مستنداً حفظتَه للتو.
- `open_exported` لا تكتب شيئاً: حين تضغط **فتح** بجانب ملف حفظته لوحة الفيديو للتو،
  تفتح ذلك الملف في التطبيق الذي يفتحه به نظامك — الفيلم في مشغّل الفيديو لديك. ولا
  تفتح إلا مساراً كتبته `export_write_video` منذ بدء تشغيل التطبيق، ولا إلا ملف
  .mp4 أو .webm أو .png أو .srt، ولا إلا ما دام ملفاً عادياً يبدأ كما بدأ حين كُتب؛
  ولا تفتح أبداً ملف مخطط المشاهد، ولا مجلداً ولا رابطاً، ولا أي شيء يُشغَّل.
- `history_restore` و`checkpoint_restore` و`checkpoint_redo` — إعادة ملف إلى نسخة
  سجّلها هذا التطبيق سلفاً، من لوحة سجل الملف أو من زرّ تراجع.
- `draft_save` و`draft_clear` و`checkpoint_save` و`store_empty` — هذه تكتب داخل
  مجلد بيانات التطبيق، لا في مشروعك أبداً. و`store_empty` هو الوحيد الذي يحذف
  فقط: إنه قسم التخزين في الإعدادات وهو يُفرغ مخزناً كاملاً، ويأخذ تعداداً من
  ثلاث قيم لا مساراً، فليس هناك اسم مجلد يمكن توجيهه إليه. انظر *ما الذي
  يُخزَّن خارج مشروعك*.

**والنموذج لا يستطيع استدعاءها.** مخطط الأدوات في `app/src/agent.ts` يحوي ثمانية
أسماء بالضبط:

```
للقراءة فقط:  list_tree    read_file   find_symbol   search
والبقية:      write_file   edit_file   run_command   remember
```

`apply_write` ليس بينها. وكذلك ليس بينها `create_file` ولا `create_dir` ولا
`rename_path` ولا `delete_path` ولا `git_create_branch` ولا `git_commit` ولا
`export_write` ولا `export_write_docx` ولا `export_write_pptx` ولا `export_write_video` ولا `save_pdf` ولا `reveal_path` ولا `open_exported` ولا `draft_save` ولا `draft_clear` ولا `history_restore` ولا
`history_forget` ولا `history_forget_all` ولا `checkpoint_save` ولا
`checkpoint_restore` ولا `checkpoint_redo` ولا
`store_sizes` ولا `store_empty` ولا
`capture_screenshot` ولا `set_global_shortcut` ولا `watch_start` ولا
`watch_stop` ولا `mcp_start` ولا `mcp_stop` ولا `mcp_call` ولا أيٌّ من
`pty_open` / `pty_write` / `pty_resize` / `pty_close`. كل واحد من هذه موجود في
Rust ولا يمكن بلوغه إلا من زرّ يضغطه إنسان.

وهذا ليس وصفاً للحال الراهن، بل تأكيد مكتوب في `app/test/modes.test.mjs`، يُفشل
`npm test` إن ظهر أي من تلك الأسماء في المخطط — ويُفشله أيضاً إن أُضيفت أداة
جديدة لا تنضم إلى نصف القراءة ولا إلى النصف الآخر، حتى لا يمكن تخطّي السؤال.
(البوابة التي يُسقطها هي `npm test` لا `npm run build`، فهذا الأخير `tsc --noEmit`
ثم Vite ولا يشغّل أي اختبار.) والفحص نفسه قدرة *غائبة*، لا تعليمة في موجّه.

**`write_file` و`edit_file` لا يكتبان.** بل يحسبان المحتوى الناتج ويتركانه في
منطقة تحضير (`app/src/pending.ts`)؛ يظهر الفرق في لوحة المراجعة ولا يُمسّ القرص
حتى توافق. ويمكنك الموافقة على ملف كامل أو على أجزاء منفردة
(`app/src/Review.tsx` و`Pending.applyPartial`). و`remember` هو الآلية نفسها
موجَّهة إلى `VYLO.md`، فالمعلومة التي يريد الوكيل حفظها هي فرقٌ تقرأه أولاً.

**ولا يمكن أن تتقادم موافقة من وراء ظهرك.** يأخذ `apply_write` وسيط
`expect_sha256` ويرفض الكتابة إن تغيّر الملف منذ تحضير الاقتراح، بدل أن يطمس ما
فعلتَه في هذه الأثناء. و`Pending.apply` يمرّر بصمة النسخة التي بُني عليها الفرق؛
أما الملف الجديد فيمرّر بصمة فارغة تعني «يجب ألّا يكون هذا موجوداً بعد». يثبت
ذلك الاختبار
`apply_write_refuses_a_file_that_moved_since_the_change_was_prepared` في
`lib.rs`.

**ولا شيء يخرج من المجلد المفتوح.** كل مسار يقدّمه النموذج يمرّ عبر `resolve()`
في `lib.rs`، وهي تُقنّن الجذر والهدف معاً وترفض أي مسار لا يبدأ بالجذر — فـ `..`
والروابط الرمزية تُحلّ فعلياً بدل مطابقتها كنمط نصّي، والملف غير الموجود بعد
يُفحص عبر مجلده الأب.

وهناك **أحد عشر** استثناءً مقصوداً، والجامع بينها أن المسار مسارٌ اخترتَه أنت — أو،
في التنزيل، مجلد التنزيلات لديك — لا مسارٌ قدّمه النموذج. فـ `read_image` و`read_text_attachment` و`read_document`
و`read_any_file` تقرأ ملفاً سحبتَه بنفسك أو اخترته من نافذة اختيار — وكلٌّ منها
يقول ذلك في تعليقه داخل `lib.rs`؛ ومن الثلاثة الأخيرة تأتي ملفات البيانات في لوحة
البحث العلمي. و`export_write` و`export_write_docx` و`export_write_pptx` و`export_write_video` *تكتب* إلى مسار مطلق، وهو
مسار نافذة الحفظ (أو، في تنزيلات لوحة الفيديو، مسار في مجلد التنزيلات)، وكذلك
`save_pdf`؛ و`reveal_path` تُظهر مساراً في Finder أو المستكشف، و`open_exported`
تفتح ملفاً كتبته لوحة الفيديو للتو. والأحد عشر جميعاً غائبة عن مخطط الأدوات، فلا
يبلغ أياً منها نداءُ أداة مهما وُجِّه النموذج.

**حدود الحجم.** لا يقرأ الوكيل ملفاً يتجاوز 512 كيلوبايت. ويعرض المحرّر أول 2
ميغابايت من الملف الأكبر ثم يصير للقراءة فقط، لأن حفظ مخزن يحمل رأس الملف وحده
كان سيتلف بقيته بصمت.

---

## ما الذي يستطيع تشغيل أمر

`run_command` **موجود** في مخطط الأدوات، وهذا هو التصميم لا سهو فيه: للنموذج أن
يطلب. ثم يُعلَّق طلبه حلقةَ الوكيل على وعدٍ حتى تجيب أنت (`askToRun` في
`app/src/App.tsx`، و`runTool` في `app/src/agent.ts`). تُعرض عليك السلسلة بحرفها،
والسبب المذكور لها، والمجلد الذي ستعمل فيه. ولا شيء يُوسَّع أو يُعاد كتابته أو
يُؤوَّل بين ذلك الحوار والصدفة.

الصدفة هي `sh -c` على macOS و`cmd /C` على Windows، داخل المجلد المفتوح، بمهلة
افتراضية 120 ثانية، ومع اقتصار المخرجات على آخر 60,000 حرف من كل مجرى، وقتلٍ
للعملية إن تجاوزت المهلة (`run_command` في `lib.rs`). لا توجد قائمة سماح بأوامر
بعينها. البوابة هي أنك قرأتَه.

**«اسمح دائماً بهذا»** موجود، وهذا ما يفعله بالضبط: يتذكّر تلك السلسلة الواحدة
لبقية الجلسة، بمطابقة حرفية، في الذاكرة فقط. لا تُكتب على القرص أبداً، وتُمسح
حين تفتح مجلداً آخر، والثقة بـ `npm test` لا تعني الثقة بـ
`npm test && rm -rf ~` (المتغيّر `trusted` في `App.tsx`).

**«تشغيل في الطرفية»** يضع السلسلة نفسها — الموافَق عليها سلفاً — على سطح تراه
وتستطيع مقاطعته. وليس قراراً ثانياً.

**نداءات أدوات MCP تمرّ بالحوار نفسه.** يعيش `.vylo/mcp.json` داخل المشروع، أي
أنه يصل مع المشروع، ومستودع استنسختَه يستطيع تسمية أي أمر شاء — ولهذا فإن قراءة
ذلك الملف لا تشغّل شيئاً. لا ينطلق خادم إلا من زرّ، بعد أن يكون أمره بحرفه على
الشاشة، وتُحفظ الموافقة مقابل بصمة الأمر ووسائطه، فتعديل الإعداد يسأل من جديد
(`app/src/mcp.ts` و`app/src-tauri/src/mcp.rs`). ثم يمرّ نداء أي أداة من أدواته
بالحوار أعلاه، لأن آثار أداة طرف ثالث لا تُعرف من اسمها.

**واقرأ التحفّظ أعلاه مرّتين حين يتعلق الأمر بـ MCP.** فالحوار يعرض **«اسمح دائماً
بهذا»** لنداء MCP تماماً كما يعرضه لأمر صدفة، ويتصرّف بالطريقة نفسها: يتذكّر
سلسلة واحدة بحرفها، وهي في حالة MCP `server: tool({"the":"arguments"})`
التي يبنيها `runTool` في `app/src/agent.ts`، في الذاكرة وحدها، حتى تفتح مجلداً
آخر. فتكرار نداء سمحتَ به — الخادم نفسه والأداة نفسها والوسائط نفسها بايتاً
ببايت — يعمل بعدها دون سؤال، وأي اختلاف بحرف واحد يسأل.

### لماذا لا تملك الطرفية خطوة موافقة

الطرفية المدمجة (`app/src-tauri/src/pty.rs`) تشغّل صدفة دخول حقيقية بلا أي
بوابة، وهذا متّسق مع القاعدة لا استثناء منها. فالقاعدة عن *مَن يؤلّف* الأمر.
أنت حين تكتب في صدفة على جهازك أنت هو المؤلّف؛ ومطالبتك بالموافقة على ضغطاتك
مسرحية، تماماً كما ستكون في محرّر الشيفرة أو محرّر الذاكرة.

ما يحفظ الخط هو أن النموذج لا يستطيع الكتابة فيها. `pty_open` و`pty_write` و
`pty_resize` و`pty_close` غائبة عن مخطط الأدوات. والجسر باتجاه واحد: تستطيع أنت
الضغط على **إرسال إلى المحادثة** لتسليم مخرجات الطرفية إلى الوكيل، ولا يوجد
عكسه عن قصد. والسلسلة الوحيدة المؤلَّفة من النموذج التي يمكن أن تصل إلى طرفية
هي سلسلة وافقتَ عليها في الحوار ثم اخترتَ تشغيلها هناك.

### الطرفية تقرأ سجلّ أوامر صدفتك

منذ 0.80.0 تُغذّى قائمة الإكمال من ملف سجلّ صدفتك نفسه — `$HISTFILE` إن كنت قد
صدّرته، ثم `~/.zsh_history` و`~/.bash_history`
و`~/.local/share/fish/fish_history`، أوّل ملف فيه شيء. هذا ما يجعل القائمة قادرة
على إتمام `claude --dang` إلى سطر شغّلته خمسين مرة، بدل الاقتصار على أسطر كتبتها
في تلك اللوحة منذ فتحها.

ثلاثة أمور عنه:

- **يقرأ فقط.** ذلك الملف ملك للصدفة، وهي تعيد كتابته عند الخروج، وتطبيق يعدّله
  هو تطبيق يفسد سجلّك ليوفّر عليك أربع ضغطات. `shell_history` في `pty.rs` يفتحه
  للقراءة فقط، ولا يوجد كاتب له في أيّ مكان من هذا التطبيق.
- **لا شيء يغادر الجهاز.** الأسطر تذهب إلى قائمة على الشاشة، في نافذة صاحب
  السجلّ. لا طلب في الجدول أعلاه يحملها، والنموذج لا يُعطاها أبداً —
  `shell_history` و`shell_commands` و`complete_path` غائبة عن مخطّط الأدوات تماماً
  كما هي أوامر `pty_*`.
- **الأسطر التي تبدو حاملةً لبيانات اعتماد تُسقَط** — `PASSWORD=` و`TOKEN=`
  و`Authorization:` و`mysql -pHunter2` و`curl -u user:pass` وما شابه. اقرأ
  `looks_secret` قبل أن تثق بتلك الجملة: إنه مطابقة أنماط على سطور أوامر
  عشوائية وسيفوته أشياء. ما لا يستطيع فعله هو جعل الأمر أسوأ، لأن سطراً يفوته
  يُعرض تماماً كما يعرضه سهمك العلوي. هو موجود لأن الاقتراح يظهر *دون طلب*، بعد
  أربع ضغطات، والسهم العلوي لا يفعل.

إن كنت تفضّل ألّا يقرأ شيئاً، فالملف الذي يقرأه ملكك: وجّه `HISTFILE` إلى
`/dev/null` لصدفة التطبيق، أو امسح ملف السجلّ.

زرّ لقطة الشاشة يبدأ عملية دون سؤال، ويستحق ذلك لأنه لا يملك ما يُوافَق عليه: كل
راية ثابتة عند الترجمة، والنمط تعداد من قيمتين لا سلسلة نصية، فلا يوجد وسيط يمكن
لشيء أن يصل عبره (`app/src-tauri/src/capture.rs`). وأنت من يسحب المستطيل، وهذا ما
يجعل اللقطة لقطتك.

وعلى macOS البرنامج مسار مطلق، `/usr/sbin/screencapture`، ويشرح `capture.rs`
السبب في تعليق: التطبيق يرث بيئتك، فوجود `screencapture` أسبق في `PATH` كان
سيعني تشغيل برنامج اختاره غيرك بلا أي موافقة. أما على ويندوز فالخطة
`cmd /C start "" ms-screenclip:`، و`cmd` هناك **يُحلّ** عبر `PATH` كأي إطلاق عملية
أخرى في ويندوز.

---

## ما الذي يُخزَّن خارج مشروعك

ثلاثة مخازن تعيش في مجلد بيانات التطبيق، لا في المجلد الذي تحرّره — فأن يظهر
سجلّ تغيير كتغييرٍ آخر عبثٌ:

- **macOS** — `~/Library/Application Support/com.vylo.editor`
- **Windows** — `%APPDATA%\com.vylo.editor`

ذلك هو `app_data_dir()` في Tauri لمعرّف هذا التطبيق — الدالة `store()` في
`app/src-tauri/src/lib.rs` تطلبه، والحقل `identifier` في `tauri.conf.json` هو ما
يسمّيه.

قسم **التخزين** في **الإعدادات** يعرض الأربعة جميعاً، ويقول كم يشغل كلٌّ منها،
ويُفرغ أياً منها خلف تأكيد يسمّي ما سيذهب. وحتى 0.52.1 لم يكن لمخزن نقاط
الاستعادة زرٌّ كهذا، وكان العمود الأخير في هذا الجدول يقول ذلك. أما الآن فله زر.

| المخزن | ما يحفظه | الحدّ | كيف تمسحه |
|---|---|---|---|
| `drafts/` | المخازن غير المحفوظة في المحرّر، كي لا يضيعها انهيار | تُكنس بعد 30 يوماً دون مساس | **إفراغ** في قسم التخزين بالإعدادات؛ و**تجاهلها** في شريط الاسترجاع؛ وتُمسح أيضاً عند خروج نظيف |
| `checkpoints/` | محتوى الملفات قبل كتابة موافَق عليها، مع ذيل المحادثة، ليعيد التراجعُ الاثنين | 60 لكل محادثة، و32 ميغابايت لكل محادثة، وتُزال مجلدات المحادثات بعد 30 يوماً دون مساس | **إفراغ** في قسم التخزين بالإعدادات |
| `history/` | كل نسخة كتبها هذا التطبيق، ليبقى الحفظ فوق عملك قابلاً للاسترجاع | 32 ميغابايت لكل مجلد، و30 يوماً؛ والنسخة الواحدة فوق 4 ميغابايت لا تُسجَّل | **إفراغ** في قسم التخزين بالإعدادات؛ أو **نسيان سجل هذا الملف** / **نسيان كل شيء في هذا المجلد**، في لوحة سجل الملف |

**وسجل الحافظة ليس منها**، وهذا ما يهمّ إن كنتَ جئتَ إلى هذا القسم لتمسحه. فهو
محفوظ في `localStorage` الخاص بالـ webview تحت المفتاح `vylo.clips.v1`
(`localClips` في `app/src/clips.ts`) — 20 مدخلاً، و7 أيام، و4096 حرفاً لكل مدخل،
ويُمسح من **إفراغ** في قسم التخزين بالإعدادات أو من **مسح** في منتقي الحافظة.
وحذف مجلد بيانات التطبيق لا يمسحه.

ويستحق قراءة ثانية: **إنه لا يستطلع حافظة النظام.** السجل الذي
يستطلع يسجّل ما وضعه مدير كلمات المرور هناك قبل ثلاثين ثانية. وهذا السجل لا
يسجّل إلا ما تلصقه *داخل هذا التطبيق*، ويتخطّى اللصق في حقول كلمات المرور،
ويتخطّى ما وسمه المصدر بأنه مُخفى، ويتخطّى السلاسل التي تشبه الأسرار في شكلها.
وهذا الفحص الأخير مجاملة لا ضمانة — فهو يطابق أشكالاً، ولذلك تفوته كلمة مرور
قصيرة أو عبارة من أربع كلمات، وملف `app/src/clips.ts` يقول ذلك في مقدمته.

**وجلسات الطرفية ليست منها أيضاً**، وهذه تستحق الانتباه. لكي تعود طرفيات
المشروع عند إعادة فتحه، يُحفظ ذيل ما كان على شاشة كل طرفية في `localStorage`
تحت المفتاح `vylo.terminals.v1` (`app/src/scrollback.ts`) — 8 جلسات لكل مجلد،
240 سطراً لكل جلسة، و120,000 حرف و8 مجلدات إجمالاً، ويُفرَغ بـ**إفراغ** في
الإعدادات ← التخزين.

وهذا هو *ما طبعته أوامرك*: `cat` لملف إعدادات، أو رمز طبعته أداة سطر أوامر، أو
مخرجات `env`. لا شيء يصفّيه، لأن لا شيء يستطيع ذلك بموثوقية. فإن مرّ شيء حساس
عبر طرفية، أفرغ هذا المخزن.

أمّا العمليات نفسها فلا تُستعاد أبداً ولا يمكن أن تُستعاد — الصدفة ابنٌ لهذا
التطبيق وتموت معه. الطرفية المستعادة تعرض ما كان فيها، ثم سطراً يقول إن الصدفة
جديدة وإن لا شيء فوقه يعمل، ثم موجّهاً جديداً. وذلك السطر غير قابل للإخفاء: نصٌّ
سابق فوق موجّه حيّ بلا فاصل بينهما هو عملية ميتة ترتدي ثياب عملية حيّة.

**ومستندات البحث العلمي ليست منها أيضاً.** لكي تنجو أطروحةٌ كُتبت على مدى أسابيع
من إغلاق اللوحة أو التطبيق، تُحفظ مسودة كل مستند — الطلب، وملاحظاتك وبياناتك، ونصّ ملفات بياناتك، ونسخة من بيانات الغلاف، والهيكل، والنص المكتوب
حتى الآن، والمراجع التي عُثر عليها — في IndexedDB الخاص بالـ webview، في قاعدة
بيانات اسمها `vylo-research`، على هذا الجهاز (`app/src/researchstore.ts`). وحذف مستند في اللوحة يحذفه من هناك.
أما بيانات الغلاف التي يملؤها الباحث مرة واحدة — الاسم، والمشرف، والجامعة وما
شابه — فتُحفظ في `localStorage` تحت `vylo.research.profile.v1` (`PROFILE_KEY` في
`app/src/research.ts`).
أما شعارات الجامعات، إن أضفتَها، فصورٌ تختارها بنفسك من نافذة اختيار الملفات في
النظام؛ وتُحفظ في `localStorage` تحت `vylo.research.logo.v1`، شعارٌ لكل جامعة
وثمانية على الأكثر، يُحذف أقدمها أولاً (`LOGO_LIBRARY`)، وفي كل مستند وُضع عليه أحدها. وملفات البيانات التي ترفقها تُختار من نافذة اختيار
الملفات أيضاً، ويفكّ `app/src/researchdata.ts` ملف Word أو Excel داخل الصفحة؛ ولا
يُحفظ منها إلا النص المقروء منها، في مسودة المستند.

**والفيديوهات ليست منها أيضاً.** يُحفظ كل فيديو — ما طلبتَه، ومخطط المشاهد
وكلماته، وألوان العلامة التجارية وشعارها اللذان أعطيتَه إياهما، والحقائق والصور
التي عُثر عليها عن موضوعه، والصور والموسيقى التي جُلبت له مع نِسبها، وتسجيل
تعليقه الصوتي — في IndexedDB الخاص بالـ webview، في قاعدة بيانات اسمها
`vylo-video`، على هذا الجهاز (`app/src/videostore.ts`). وحذف فيديو في اللوحة
يحذفه من هناك. أما الفيلم أو صورة الغلاف أو الترجمة النصية أو ملف مخطط المشاهد
فلا يُكتب إلا حين تضغط الزر الخاص به — في مجلد التنزيلات، باسم لا يحمله ملف آخر
هناك، أو حيث تختار مع **حفظ بصيغة MP4…** — ولا يُكتب إلا هناك. وتُحفظ الدقة ومعدّل
البت والصوت التي اخترتها آخر مرة للتنزيل في `localStorage`
(`vylo.video.download`).

**والعروض التقديمية ليست منها أيضاً.** يُحفظ كل عرض تقديمي — ما طلبتَه، وشرائحه
وملاحظات المتحدث، وألوانه، والأسماء والشعار على شريحة عنوانه، وللعرض المأخوذ من
مستند في البحث العلمي: الجزء الذي كُتب منه من ذلك المستند وقائمة مصادره — في
IndexedDB الخاص بالـ webview، في قاعدة بيانات اسمها `vylo-slides`، على هذا الجهاز
(`app/src/slidestore.ts`). وحذف عرض تقديمي في اللوحة يحذفه من هناك. أما أسماء
شريحة العنوان وشعارها، التي تُملأ مرة للعرض التالي، فتُحفظ في `localStorage` تحت
`vylo.slides.cover.v1`. ولا يُكتب ملف PowerPoint أو PDF إلا حين تضغط **حفظ بصيغة
PowerPoint** أو **حفظ بصيغة PDF**، ولا يُكتب إلا حيث تختار.

أمّا المحادثات والإعدادات ومفتاح البوابة ورمز جلستك وأي خوادم MCP فعّلتَها وأي
مزوّدي نماذج أضفتَهم وسجل الحافظة وجلسات الطرفية وبيانات غلاف البحث العلمي والعروض التقديمية
أعلاه فهي في `localStorage` الخاص بالـ webview، ومسودات البحث العلمي والفيديوهات والعروض التقديمية في
IndexedDB الخاص به — لا في ذلك المجلد.

ولا شيء هنا مشفَّر في السكون أبعد ممّا يفعله قرصك أصلاً.

---

## ما الذي لا يحميك منه Vylo Editor

هذا هو القسم الذي يقرّر إن كان بقية المستند يساوي شيئاً.

**الأمر الذي توافق عليه يعمل بصلاحياتك أنت.** لا صندوق رملي، ولا حاوية، ولا
قائمة سماح. `sh -c` داخل مجلد مشروعك، بصلاحيات مستخدمك، يستطيع حذف مجلد المنزل،
والدفع إلى مستودع بعيد، وقراءة مفاتيح SSH، وتثبيت أي شيء. البوابة هي أنك قرأت
السلسلة أولاً. فإن وافقتَ على `curl … | sh` فقد شغّلتَ ما قدّمه ذلك الرابط، وقد
فعل Vylo تماماً ما أمرتَه به.

**الفرق المُحضَّر الذي توافق عليه دون قراءة هو فرق كتبتَه أنت.** المراجعة
بالأجزاء، وحارس الكتابة المتقادمة، ونقاط الاسترجاع، كلها تفترض أن أحداً نظر. ولا
واحد منها يميّز موافقة متأنّية من موافقة سريعة، وفرقٌ طويل في آخر جلسة طويلة هو
بالضبط موضع الفشل.

**حقن الموجّه حقيقي ولم يُحلّ.** يستطيع مستودع أن يحوي نصاً موجَّهاً إلى الوكيل
لا إليك — في ملف README، أو في تعليق، أو في بيانات اختبار، أو في مشكلة لصقتَها.
ما يصمد هو البنية: الاحتواء مفروض في Rust، والأفعال الخطرة تحتاج إنساناً. وما لا
يصمد هو حُكم النموذج، فقد يصل طلبٌ غير معقول إلى حوار الموافقة وهو يبدو معقولاً
تماماً.

**خادم MCP تفعّله هو شيفرة طرف ثالث تعمل بصلاحياتك.** إنه برنامج سُمّي في ملف
وصل مع المستودع. يعرض عليك Vylo أمره قبل تشغيله ويضع بوابة أمام كل نداء أداة،
لكنه ما إن يعمل حتى يصير عملية على جهازك بصلاحياتك، وقادرة على الوصول إلى
الشبكة بنفسها — فسياسة أمان المحتوى أعلاه تقيّد نافذة Vylo، لا عملية ابنة.

**النموذج قد يخطئ.** بثقة، وبمعقولية ظاهرة، وفي وسط عمل صحيح فيما عداه. قد يسيء
قراءة شيفرة، أو يُسقط سطراً كان مهمّاً، أو يزعم أنه شغّل اختباراً لم يتجاوز
اقتراحه، أو يلخّص ملفاً لم يُكمل قراءته. يجعل Vylo ناتجه قابلاً للمراجعة. ولا
يجعله صحيحاً.

**Git ما يزال هو تراجعك.** نقاط الاسترجاع وسجل الملف راحتان محليتان محدودتان —
محدودتان بالحجم، ومحدودتان بالعمر، وعلى هذا الجهاز وحده. وليستا نسخة احتياطية.

**التطبيق ليس مُحصَّناً ضد بوابة مخترَقة** أبعد من توقيع التحديث. البوابة التي
تجيب طلباتك تستطيع أن تجيبها بأي شيء. وما لا تستطيعه هو دفع بناء غير موقَّع
إليك، أو بلوغ ملفاتك دون المرور بحوار رأيتَه أنت.

**الحِزم ليست موقَّعة بعد.** حتى الإصدار 0.114.0 لم تُوقَّع ثنائيات macOS و
Windows ولم تُوثَّق، فسيحذّر منها Gatekeeper وSmartScreen. وذلك التحذير صحيح:
تحقّق من مصدر التطبيق قبل أن تتجاوزه.

**لم يُدقَّق أي من هذا.** لم تُجرَ أي مراجعة أمنية من طرف ثالث لهذا التطبيق.

---

## كيف تبلّغ عن مشكلة أمنية

رجاءً لا تفتح مشكلة عامة.

بلّغ سرّاً عبر نموذج التنبيهات الأمنية في GitHub على المستودع:
<https://github.com/AvrazAkraye/vylo-editor/security/advisories/new>. يصل ذلك
إلى القائم على المشروع ويبقى خاصاً حتى يُشحن الإصلاح.

اذكر ما فعلتَه، وما حدث، ورقم الإصدار الظاهر في **الإعدادات**. وإن وجدتَ طريقاً
يجعل ناتج النموذج يصل إلى القرص أو إلى الطرفية دون أن يوافق إنسان على ذلك
المحتوى أو تلك السلسلة بعينها، فتلك هي العلّة التي يعنى بها هذا المنتج أكثر من
غيرها — وتستحق البلاغ حتى لو لم تكن واثقاً من إمكان استغلالها.

---

*آخر تحقّق مقابل الإصدار 0.114.0. كل جملة أعلاه قُرئت من الشيفرة. وإن اختلفت
الشيفرة يوماً مع هذا المستند، فالشيفرة هي الصواب وهذا المستند هو العلّة.*
