{"default":{"identifier":"default","description":"What the window is allowed to do. Deliberately small: folder picking, and the three read-only tool commands. The global-shortcut plugin is initialised in lib.rs but its own commands are deliberately not listed here, so the only chord that can ever be registered is the one a person set in Settings, through set_global_shortcut.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open","updater:default","process:allow-restart","core:window:allow-set-fullscreen","core:window:allow-is-fullscreen","core:window:allow-set-theme","core:window:allow-start-dragging","core:window:allow-destroy"]}}